# Musk Intel Dashboard

这是从 `musk_intel_v3_1.jsx` 恢复出来的 Vite React 本地项目。

## 文件说明

- `musk_intel_v3_1.original.jsx`：原始备份文件，未修改。
- `src/App.jsx`：网站主组件，由原始 JSX 文件复制而来。
- `src/main.jsx`：React 启动入口，用来把 App 渲染到网页里。
- `index.html`：浏览器打开的网页入口。
- `package.json`：项目依赖和启动命令。

## 电脑本地预览步骤

先安装 Node.js，然后在项目文件夹里运行：

```bash
npm install
npm run dev
```

终端会显示一个本地地址，通常是：

```text
http://localhost:5173
```

复制这个地址到浏览器即可预览。

## 给 Codex / Claude Code 的建议

请先运行项目，不要重写内容。若报错，只修复运行环境或明显语法问题。任何大改之前先说明会改哪些文件。
